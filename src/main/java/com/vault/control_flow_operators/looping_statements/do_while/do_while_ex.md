## 01
```
// step 1. выполняется декремент(уменьшает значение на 1)
// step 2. полученное значение возвращается переменной `n`
// step 3. полученнае значение `n` сравнивается с нулем 
int n = 10;  
do {  
	System.out.println("n =" + n);  
	n--;  
} while (n > 0);
```
